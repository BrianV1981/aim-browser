---
name: aim-google-web
description: >
  Classic Google web/SERP results via udm=14 (not AI Mode). Returns ranked titles, URLs, snippets as markdown+JSON.
  Live only; minimized; closes Chromium after session by default.
---

# aim-google-web

Use when you need **blue links**, not Google AI Mode prose.

```bash
npm run google-web -- "best static site generators 2026"
```

For AI Mode answers use **aim-google-ai** instead (`udm=50`).
