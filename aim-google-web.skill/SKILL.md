---
name: aim-google-web
description: >
  Classic Google web results (udm=14), not AI Mode. Ranked titles, URLs, snippets
  as markdown + JSON. Live only.
---

# aim-google-web

Use for **blue links**, not AI Mode prose.

```bash
npm run google-web -- "best static site generators"
```

AI Mode → **aim-google-ai**. Defaults: minimized, stop after session.
